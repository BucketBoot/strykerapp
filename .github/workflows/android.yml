name: Build StrykerApp APK

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 11
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '11'

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Cache Gradle dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: Build Debug APK
      run: ./gradlew assembleDebug

    - name: Upload Debug APK as artifact
      uses: actions/upload-artifact@v4
      with:
        name: StrykerApp-Debug-APK
        path: app/build/outputs/apk/debug/*.apk

    # Для сборки подписанной релизной версии (опционально)
    # - name: Decode Keystore
    #   if: github.ref == 'refs/heads/main' && secrets.RELEASE_KEYSTORE_BASE64 != ''
    #   run: |
    #     echo "${{ secrets.RELEASE_KEYSTORE_BASE64 }}" | base64 --decode > app/keystore.jks
    # 
    # - name: Build Signed Release APK
    #   if: github.ref == 'refs/heads/main' && secrets.RELEASE_KEYSTORE_BASE64 != ''
    #   run: ./gradlew assembleRelease
    #   env:
    #     STRYKER_RELEASE_STORE_FILE: app/keystore.jks
    #     STRYKER_RELEASE_STORE_PASSWORD: ${{ secrets.RELEASE_STORE_PASSWORD }}
    #     STRYKER_RELEASE_KEY_ALIAS: ${{ secrets.RELEASE_KEY_ALIAS }}
    #     STRYKER_RELEASE_KEY_PASSWORD: ${{ secrets.RELEASE_KEY_PASSWORD }}
    # 
    # - name: Upload Signed Release APK as artifact
    #   if: github.ref == 'refs/heads/main' && secrets.RELEASE_KEYSTORE_BASE64 != ''
    #   uses: actions/upload-artifact@v4
    #   with:
    #     name: StrykerApp-Release-APK
    #     path: app/build/outputs/apk/release/*.apk
